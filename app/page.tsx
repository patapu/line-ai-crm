// OWNER: lane A (placeholder from layer 0)
//
// Real page is the pipeline board (docs/design.md section 1:
// `(app)/page.tsx [A] pipeline board`). This placeholder only exists so
// `next typegen` / `next build` have a valid app tree in layer 0.

export default function HomePage() {
  return null
}
